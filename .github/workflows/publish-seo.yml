name: Publish SEO Content

on:
  schedule:
    # 4 runs/day × 13 EN + 13 ES = ~52 per locale per day (~50 target)
    - cron: "0 8,14,20,2 * * *"
  workflow_dispatch:
    inputs:
      enrich_limit:
        description: "City markdown files to enrich (0 = skip)"
        default: "3"
      run_indexnow:
        description: "Submit IndexNow after publish"
        type: boolean
        default: true

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "20"

      - name: Daily SEO automation (enrich + reports)
        run: |
          ENRICH="${{ github.event.inputs.enrich_limit || '3' }}"
          if [ "$ENRICH" = "0" ]; then
            node scripts/seo-daily.mjs --skip-enrich
          else
            node scripts/seo-daily.mjs --enrich "$ENRICH"
          fi

      - name: Generate city data
        run: node scripts/generate-city-data.mjs

      - name: Generate English blog drafts
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          BLOG_POSTS_PER_DAY: "13"
          BLOG_LOCALE: "en"
        run: node scripts/generate-blog-batch.mjs

      - name: Generate Spanish blog drafts
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          BLOG_POSTS_PER_DAY: "13"
          BLOG_LOCALE: "es"
        run: node scripts/generate-blog-batch.mjs

      - name: Enrich and publish quality drafts (EN + ES)
        run: npx tsx scripts/enrich-blog-drafts.ts --publish --all-locales

      - name: Publish remaining drafts from queue
        run: node scripts/publish-from-queue.mjs

      - name: Repair thin drafts
        run: node scripts/repair-blog-drafts.mjs

      - name: Quality gate (drafts — non-blocking)
        run: node scripts/quality-gate.mjs --warn-only

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add content/blog/ ai-visibility-accelerator/output/content/city-posts/ data/publish-queue.json data/cities.generated.json content/blog/queue.json
          git diff --staged --quiet || git commit -m "chore(seo): daily publish + enrichment run"
          git push

      - name: Submit IndexNow
        if: github.event.inputs.run_indexnow != 'false'
        env:
          INDEXNOW_KEY: ${{ secrets.INDEXNOW_KEY }}
        run: |
          node scripts/submit-indexnow.mjs all --fallback || true
